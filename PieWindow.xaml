<Window x:Class="WinMediaPie.PieWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinMediaPie"
        mc:Ignorable="d"
        Topmost="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        AllowsTransparency="True"
        Title="PieWindow" Height="450" Width="200">
    <Window.Background>
        <SolidColorBrush Color="#FFB0B0B0"
                         Opacity="0.05" />
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Width="40" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center" Source="pack://application:,,,/Assets/speaker.png"/>
        <ToggleButton Checked="Mute_Checked" Unchecked="Mute_Unchecked" Width="30" Height="30" Grid.Row="1">
            <ToggleButton.Background>
                <SolidColorBrush Opacity="0.5" Color="#FFB0B0B0"/>
            </ToggleButton.Background>
            <Image x:Name="muteButtonIcon" VerticalAlignment="Center" HorizontalAlignment="Center" Width="20" Height="20" Source="pack://application:,,,/Assets/volume-off.png" />
        </ToggleButton>
        <Slider Margin="0, 10, 0, 10" Grid.Row="2" local:MouseWheelBehavior.Value="3" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
    </Grid>
</Window>
